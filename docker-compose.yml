version: '3.7'

# Services
services:

  # Nginx Service
  nginx:
    image: nginx:1.17
    ports:
      - 80:80
    environment:
      - NGINX_SERVER_NAME=${LOCAL_DOMAIN}
    volumes:
      - ./src:/var/www/:ro
      - ./.docker/nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - perch
    command: /bin/bash -c "envsubst '$$NGINX_SERVER_NAME' < /etc/nginx/conf.d/perch.template > /etc/nginx/conf.d/perch.conf && nginx -g 'daemon off;'"

  # Perch CMS
  perch:
    build: ./.docker/perch
    working_dir: /var/www/
    volumes:
      - ./src:/var/www/
    depends_on:
      - database

  # MySQL Service
  database:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${COMPOSE_PROJECT_NAME}
      MYSQL_USER: ${PERCH_DB_USERNAME}
      MYSQL_PASSWORD: ${PERCH_DB_PASSWORD}
    volumes:
      - ./.docker/mysql/my.cnf:/etc/mysql/conf.d/my.cnf:ro
      - mysqldata:/var/lib/mysql

  # PhpMyAdmin Service
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:5
    ports:
      - 8080:80
    environment:
      PMA_HOST: database
    depends_on:
      - database

# Volumes
volumes:

  mysqldata: